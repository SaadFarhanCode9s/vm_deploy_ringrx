[template]
prefix = "/service/<%=node['rxsynapse_attributes']['pg_cluster_name']%>"
src = "haproxy.tmpl"
dest = "/etc/haproxy/haproxy.cfg"
check_cmd = "/usr/sbin/haproxy -c -f {{ .src }}"
reload_cmd = "/bin/systemctl reload haproxy"

keys = [
    "/members/",
]
